{
  "routes": [
    {
      "method": "GET",
      "path": "/calendars",
      "handler": "calendars.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/calendars/count",
      "handler": "calendars.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/calendars/:id",
      "handler": "calendars.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/calendars",
      "handler": "calendars.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/calendars/:id",
      "handler": "calendars.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/calendars/:id",
      "handler": "calendars.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
